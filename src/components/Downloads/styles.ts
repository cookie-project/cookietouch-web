import { StyleRulesCallback } from "@material-ui/core";

export type DownloadsStyle = "root";

export const downloadsStyles: StyleRulesCallback<DownloadsStyle> = theme => ({
  root: {
    flexGrow: 1
  }
});
