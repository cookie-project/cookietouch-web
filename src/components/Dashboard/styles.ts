import { StyleRulesCallback } from "@material-ui/core";

export type DashboardStyle = "root";

export const dashboardStyles: StyleRulesCallback<DashboardStyle> = theme => ({
  root: {
    flexGrow: 1
  }
});
